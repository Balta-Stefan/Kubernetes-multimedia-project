apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: {{ .Values.namespaces.backend }}
stringData:
  MYSQL_CONNECTION_STRING: jdbc:mysql://host.docker.internal:4000/media
  MYSQL_USERNAME: root
  MYSQL_PASSWORD: sigurnost